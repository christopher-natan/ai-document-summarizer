extends layout


block content
  form(action="analyze", method="post", enctype="multipart/form-data")
    <div class="container text-center mt-5">
      <p class="text-center">
          h1= title
          =description
      </p>
      <div class="col-sm mt-5">
         input(type="file", name="file_upload" class="btn btn-primary mb-3")
      </div>
      <div class="col-sm mt-2">
         input(type="submit", value="Analyze Document" class="btn btn-primary mb-3")
      </div>
    </div>

